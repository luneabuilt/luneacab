## Packages
framer-motion | Smooth animations for page transitions and UI elements
react-leaflet | Open-source maps for ride tracking without API keys
leaflet | Core mapping library
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
Using OpenStreetMap via React Leaflet for maps to avoid API key requirements for the MVP.
Firebase Auth is simulated with a mock implementation as requested for the prototype phase.
PWA manifest and service worker registration should be added to index.html/main.tsx for production.
